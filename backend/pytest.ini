[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    integration: requires a live Postgres reachable at TEST_DATABASE_URL
filterwarnings =
    ignore::DeprecationWarning:passlib.*
    ignore::DeprecationWarning:jose.*
